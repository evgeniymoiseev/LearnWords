<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.evgeniy.moiseev.learnwords">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/zzzz"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IrregularVerbsActivity"></activity>
        <activity android:name=".TrainingWordsActivity" />
        <activity android:name=".TrainingModeActivity" />
        <activity
            android:name=".WordDetailActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".DictionariesActivity"
            android:label="@string/string_dictionaries"
            android:theme="@style/DictionaryActivityTheme"
            android:windowSoftInputMode="adjustPan" />
    </application>

</manifest>